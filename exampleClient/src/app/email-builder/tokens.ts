import { InjectionToken } from '@angular/core';

export const IP_CONFIG = new InjectionToken('Ip Configuration');
export const GENERAL_OPTIONS = new InjectionToken('Email General options');
// export const DEFAULT_TEXT_BLOCK_OPTIONS = new InjectionToken(
//   'Default Text Block Options'
// );
